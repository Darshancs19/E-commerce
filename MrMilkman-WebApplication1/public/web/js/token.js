// alert function
const showAlert = (msg) => {
  // previous code
  return false;
};

const processData = (data) => {
  // previous conditions
  if(data.product){
      location.href = '/seller';
  }
}
